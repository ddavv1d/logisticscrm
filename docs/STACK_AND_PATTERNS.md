# Стек и паттерны — LogisticsCRM

> Проверенные паттерны из PKUonline/Migrebot, адаптированные под контейнерную логистику.
> Это база, которая работает. Не догма, но отклоняться — осознанно.

## Стек

- **Frontend:** React 18 + Vite + Tailwind (чистый Tailwind, НЕ Next.js, НЕ shadcn по умолчанию — компоненты из 21st.dev адаптируем под свои токены). `recharts` для графиков дашборда (ленивый).
- **Backend:** Python **3.12** + FastAPI (async) + SQLAlchemy 2.x async + Alembic + Pydantic v2.
- **DB:** PostgreSQL.
- **Auth:** email+password (**Argon2id**) + сессия в httpOnly cookie + CSRF double-submit. Без Node-тулзов на бэке.
- **Экспорт:** `openpyxl` (xlsx) — first-class фича, кнопка на каждом списке.

> ⚠️ **Грабля версий (эта машина):** системный Python — **3.14**, Node — **25**. Стек рассчитан на Python 3.12 (asyncpg/pydantic-core имеют готовые wheels). Поднимать venv на **3.12 через pyenv**, а не на системном 3.14 — иначе риск отсутствия бинарных колёс. Проверить: `python3.12 --version` перед созданием venv.

---

## Backend-паттерны (жёстко соблюдать)

### 1. Единица учёта = КОНТЕЙНЕР
Первичный объект — контейнер (его физически двигают, он «застревает»). Деньги и маржа считаются **на контейнер**. Заказ клиента = представление/фильтр, НЕ отдельная единица учёта. Это фиксирует все цифры дашборда (решение совета, риск №4).

### 2. Enum'ы как VARCHAR + CHECK, не нативный PG-enum
```python
sa.Column("transport_type", sa.String(16), nullable=False)
# + CheckConstraint("transport_type IN ('rail','sea_ferry','truck','other')")
```
Или `sa.Enum(..., native_enum=False, create_constraint=True, values_callable=lambda e:[m.value for m in e])`.
Нативный enum — боль при изменении (ALTER TYPE не откатывается в транзакции) и ломает Alembic autogenerate. **Грабля Давида из прошлых проектов — не повторять.**

> ⚠️ `[review]` **Nullable + enum-CHECK:** для nullable-колонки НЕ писать `CHECK (col IN ('a','b', NULL))` — литерал `NULL` в IN не помогает NULL проходить (это делает сама nullability), но **тихо отключает гард**: любой мусор проходит (`'GARBAGE' IN ('a', NULL)` → UNKNOWN → CHECK пропускает). Проверено рантаймом на Postgres. Правильно: `CHECK (col IS NULL OR col IN ('a','b'))`.
> **Деньги-инварианты:** `CHECK (amount > 0)` + направление задаёт `kind` (не отрицательный amount); `paid_amount` отдельным полем для 'partial'; `CHECK (currency = 'USD')` в v1 (мультивалюта = Фаза 2). **Soft-delete:** все дочерние FK — `ON DELETE RESTRICT`, не CASCADE.

### 3. Каталог стадий = Python-константа (не таблица на старте)
Список стадий коридора и типов зарядов — константы в `app/domain/stages.py`. Менеджер выбирает из них. Промотать в редактируемую таблицу stage_catalog **только когда владелец реально попросит** редактировать стадии (Фаза 2). Один источник правды, не хардкодить по роутерам.

### 4. Смена стадии = ОДНА сервисная функция, одна транзакция
Критический паттерн (риск №2 совета): рассинхрон «current_stage ≠ последняя запись журнала».
```python
async def change_stage(session, container_id, stage_code, user_id, comment, actual_date=None):
    # 1) INSERT stage_event (append-only)
    # 2) UPDATE container.current_stage_code + current_stage_since
    # ВСЁ в одной транзакции. Единственная точка входа для смены стадии.
```
Журнал `stage_event` — **строго append-only** (только INSERT, никаких UPDATE/DELETE).

### 5. Одна транзакция на запрос
Зависимость сессии делает commit на успех / rollback на ошибку. Роутеры **НЕ** делают explicit commit (иначе частичная запись при поздней ошибке). `flush()` где нужен id до коммита.

### 6. Деньги — Decimal, никогда не float
`charge_line.amount` = `Numeric(14,2)`. Маржа = Σ(income) − Σ(expense) по контейнеру — считать на Decimal. Одна колонка `currency` (default 'USD') в v1, без пересчёта курсов (мультивалюта — Фаза 2).

### 7. RBAC в сервисном слое, 3 роли + флаг видимости денег
Идентичность ТОЛЬКО из cookie-сессии. 3 роли: `manager` / `owner` / `accountant` (VARCHAR+CHECK). Флаг `can_see_money` на пользователе: менеджеру НЕ отдавать колонку маржи (разная сериализация одного list-эндпоинта по роли). **НЕ строить** field-level RBAC-движок (риск №5).

### 8. Anti-IDOR (даже без PHI — это операционные данные компании)
Каждый запрос scope по роли; для действий над записью — проверять, что запись принадлежит компании/доступна роли. Чужой/несуществующий id → 404, не «удалено». Это не медданные, но утечка чужих ставок/маржи — реальный бизнес-риск.

### 9. Soft-delete, не физическое удаление
`container.status` ∈ active/delivered/cancelled/**archived**. Владелец «сверяется и берёт выгрузки» → физическое удаление ломает аудит. Журнал stage_event неизменяем.

### 10. Миграция самодостаточна
Alembic + сид с первого дня (стадии, типовые точки коридора, типы зарядов — из Python-констант). Если используешь расширения PG — `CREATE EXTENSION IF NOT EXISTS ...` в начале upgrade(), иначе чистая БД не поднимется.

---

## Frontend-паттерны

- **Дизайн-система первой:** токены в `tailwind.config.js` (палитра «стальной коридор» — см. UX-раздел, зоны стадий, радиусы, тени) + примитивы `components/ui.jsx` (Button/Card/Field/Input/Badge/EmptyState/Skeleton/Toast).
- **Экраны из 21st.dev** (см. `21ST_MCP_GUIDE.md` + `docs/UI_COMPONENT_MAP.md`) — адаптированные под токены, не слепой копипаст.
- **api-клиент** (`lib/api.js`): fetch с `credentials:'include'` + CSRF-header из cookie; на 401 — **редирект на `/login`**. `[review]` Сессия — единая opaque cookie со скользящим TTL 12-24ч; silent-refresh/single-flight НЕ делаем в MVP (рефрешить opaque-сессию нечего, брифом не требуется — это scope-creep). Если позже введём короткий access+refresh — тогда single-flight refresh, не раньше.
- **Состояния всегда:** loading=Skeleton (не спиннер), empty=EmptyState (учит+зовёт), error=inline.
- **Смена стадии в 1-2 клика:** клик по бейджу стадии в строке → поповер со списком стадий + опц. комментарий + опц. дата факта. Синхронное сохранение + тост «сохранено». БЕЗ модалок «вы уверены?». Дата/время/автор — автоматически на бэке.
- **Наглядность маршрута:** индикатор «Плечо N из M · [текущая стадия]» + вертикальная append-only лента истории (иконка транспорта + что + автор + дата-время + комментарий, новейшее сверху).
- **Чипы-счётчики** над списком контейнеров: `[Все N] [В пути N] [Ожидание парома N] [Застряло N⚠] [Доставлено N]` — клик фильтрует. «Застряло» — янтарным.
- **Мотивированная анимация** (taste-skill): только где несёт смысл; `prefers-reduced-motion`.
- **Ленивый тяжёлый код:** recharts → `React.lazy()` + `Suspense`. `[review]` `manualChunks` добавлять **только если замер бандла покажет проблему** (не по умолчанию). Если график дашборда уйдёт в Фазу 2 — recharts в MVP не нужен вовсе.

---

## Экспорт (ключевая фича «замена Excel»)

- Один endpoint `/export.xlsx` (или `.csv`), фиксированные колонки, `openpyxl`.
- Выгружает **текущий отфильтрованный вид** (тот же фильтр, что на экране).
- Кнопка «Выгрузить в Excel» — видная, на каждом списке и дашборде. Снимает страх «мы заперты в системе».

---

## Деплой (демо — из опыта)

- Один web-сервис отдаёт SPA + API, ИЛИ 3 сервиса (Postgres + backend + frontend-Caddy с same-origin `/api`-прокси → httpOnly-cookie работают без SameSite=None, паттерн Migrebot).
- Railway monorepo: Root Directory по сервисам (`backend`/`frontend`).
- НЕ деплоить без явного согласия Давида. Демо-grade по умолчанию.
